{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "axis_aequitas_libertas",
    "axis_imperium_anarkhia",
    "axis_universalism_particularism",
    "axis_market_collective_allocation",
    "axis_inequality_acceptance_correction",
    "axis_individual_socialized_risk",
    "axis_progressivism_preservation",
    "axis_technocracy_populism",
    "conf_axis_aequitas_libertas",
    "conf_axis_imperium_anarkhia",
    "conf_axis_universalism_particularism",
    "conf_axis_market_collective_allocation",
    "conf_axis_inequality_acceptance_correction",
    "conf_axis_individual_socialized_risk",
    "conf_axis_progressivism_preservation",
    "conf_axis_technocracy_populism",
    "global_confidence",
    "flags",
    "rationale_spans"
  ],
  "properties": {
    "axis_aequitas_libertas": { "type": "number", "minimum": -100, "maximum": 100 },
    "axis_imperium_anarkhia": { "type": "number", "minimum": -100, "maximum": 100 },
    "axis_universalism_particularism": { "type": "number", "minimum": -100, "maximum": 100 },
    "axis_market_collective_allocation": { "type": "number", "minimum": -100, "maximum": 100 },
    "axis_inequality_acceptance_correction": { "type": "number", "minimum": -100, "maximum": 100 },
    "axis_individual_socialized_risk": { "type": "number", "minimum": -100, "maximum": 100 },
    "axis_progressivism_preservation": { "type": "number", "minimum": -100, "maximum": 100 },
    "axis_technocracy_populism": { "type": "number", "minimum": -100, "maximum": 100 },
    "conf_axis_aequitas_libertas": { "type": "number", "minimum": 0, "maximum": 1 },
    "conf_axis_imperium_anarkhia": { "type": "number", "minimum": 0, "maximum": 1 },
    "conf_axis_universalism_particularism": { "type": "number", "minimum": 0, "maximum": 1 },
    "conf_axis_market_collective_allocation": { "type": "number", "minimum": 0, "maximum": 1 },
    "conf_axis_inequality_acceptance_correction": { "type": "number", "minimum": 0, "maximum": 1 },
    "conf_axis_individual_socialized_risk": { "type": "number", "minimum": 0, "maximum": 1 },
    "conf_axis_progressivism_preservation": { "type": "number", "minimum": 0, "maximum": 1 },
    "conf_axis_technocracy_populism": { "type": "number", "minimum": 0, "maximum": 1 },
    "global_confidence": { "type": "number", "minimum": 0, "maximum": 1 },
    "flags": {
      "type": "array",
      "items": { "type": "string" },
      "default": []
    },
    "rationale_spans": {
      "type": "array",
      "default": [],
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["axis", "start", "end"],
        "properties": {
          "axis": {
            "type": "string",
            "enum": [
              "axis_aequitas_libertas",
              "axis_imperium_anarkhia",
              "axis_universalism_particularism",
              "axis_market_collective_allocation",
              "axis_inequality_acceptance_correction",
              "axis_individual_socialized_risk",
              "axis_progressivism_preservation",
              "axis_technocracy_populism"
            ]
          },
          "start": { "type": "integer", "minimum": 0 },
          "end": { "type": "integer", "minimum": 1 }
        }
      }
    }
  }
}
